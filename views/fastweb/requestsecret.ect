<% extend 'layouts/base.ect' %>
<div style="text-align: left">
  <div class="card" style="">
    <div class="card-block">
      <div class="alert alert-info" role="alert">
        <strong>Benvenuto nel sistema di pagamento online di Serfin97 s.r.l. per conto di Fastweb s.p.a.</strong><br />
        Prima di proseguire, &egrave; necessario verificare la tua identit&agrave;.
      </div>
      <form method="POST">
        <div class="form-group">
          <label for="askValue"><%- @askLabel %></label>
          <input type="string" class="form-control" id="askValue" placeholder="" name="askValue">
          <% if @formError :%>
            <small class="form-text text-muted text-danger">Spiacente, il codice inserito non &egrave; corretto.<br/>Prova nuovamente.</small>
          <% end %>
        </div>
        <button type="submit" class="btn btn-primary">Verifica</button>
        <% if @formError :%>
          <div class="alert alert-warning" style="margin-top: 10px">
            <strong>Hai problemi a inserire il codice?</strong><br />
            Se hai bisogno di assistenza, puoi chiamare il numero <a href="tel:192193">192193</a> per parlare con un operatore.
          </div>
        <% end %>
      </form>
    </div>
  </div>
</div>
